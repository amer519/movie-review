<h1>Editing Review</h1>

<%= render 'form', review: @review %>


<%= link_to 'Back', movie_path(@movie) %>
<%= link_to "Delete", movie_review_path(@movie, @review), method: :delete, data: { confirm: 'Are you certain you want to delete this?' } %>
